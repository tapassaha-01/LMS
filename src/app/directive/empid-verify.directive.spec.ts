import { EmpidVerifyDirective } from './empid-verify.directive';

describe('EmpidVerifyDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpidVerifyDirective();
    expect(directive).toBeTruthy();
  });
});
